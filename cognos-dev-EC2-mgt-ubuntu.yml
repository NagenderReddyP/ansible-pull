- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: Update repositories
      apt: 
        update_cache: yes
      changed_when: false

  tasks:
    - include: tasks/add_ansible_to_cron.yml
    - include: tasks/create_ansible_user.yml 
    - include: tasks/install_netcat_package.yml
